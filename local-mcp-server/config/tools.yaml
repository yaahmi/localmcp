server:
  name: "hello-world-mcp"
  version: "2.0.0"

tools:
  hello:
    enabled: true
    max_name_length: 50
  
  add:
    enabled: true
    max_value: 1000000
  
  get_time:
    enabled: true
    timezone: "Asia/Tokyo"

logging:
  level: "INFO"
  format: "structured"